# Быстрый старт - Локальное тестирование

## 🚀 За 5 минут

### 1. Создайте .env файл

```bash
cat > .env << 'EOF'
POSTGRES_DB=domos
POSTGRES_USER=domos
POSTGRES_PASSWORD=test_password_123
POSTGRES_PORT=5432
POSTGRES_HOST=localhost
DB_TYPE=postgres
BOT_TOKEN=your_bot_token_here
TZ=Europe/Moscow
EOF
```

### 2. Запустите автоматический тест

```bash
./scripts/test_local_setup.sh
```

Скрипт автоматически:
- ✅ Проверит настройки
- ✅ Запустит PostgreSQL
- ✅ Создаст схемы
- ✅ Мигрирует данные (если есть SQLite базы)
- ✅ Проверит подключение

### 3. Запустите все сервисы

```bash
docker-compose up -d
```

### 4. Проверьте работу

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f

# Проверка API
curl http://localhost:8001/docs
```

## 📋 Что дальше?

- **Подробная инструкция:** [LOCAL_TESTING.md](LOCAL_TESTING.md)
- **Развертывание на сервере:** [DEPLOYMENT.md](DEPLOYMENT.md)
- **План миграции:** [MIGRATION_PLAN.md](MIGRATION_PLAN.md)

## ⚠️ Важно

1. **Пароль PostgreSQL:** Замените `test_password_123` на безопасный пароль
2. **BOT_TOKEN:** Укажите реальный токен бота для тестирования
3. **Порты:** Убедитесь, что порты 5432, 8001, 8002, 6000 свободны

## 🔧 Решение проблем

Если что-то не работает:

```bash
# Остановить все
docker-compose down

# Посмотреть логи
docker-compose logs

# Проверить подключение вручную
python3 scripts/test_connection.py
```

Подробнее в [LOCAL_TESTING.md](LOCAL_TESTING.md)
